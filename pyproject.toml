[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-ue-studio"
version = "0.5.0"
description = "Host-agnostic, capability-routed Unreal studio orchestration"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.setuptools]
py-modules = []
