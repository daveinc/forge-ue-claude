---
name: forge-spec-phase
description: Clarify what a game project phase delivers, with ambiguity scoring, before discussion. Use when a phase goal is vague or contested.
---

# Forge Spec Phase

Delegation mode: **relay** — this workflow is interactive, so surface each question in game-dev framing and pass answers back down. Never black-box it. GSD workflow: `spec-phase.md`.

Read [delegation-contract.md](../../references/delegation-contract.md) first. It defines the PRE / CORE / POST shape, the delegation modes, and the rules not repeated here.

## PRE — Forge

1. Load the GDD ledger. Score ambiguity against settled decisions, not against a blank slate.

## CORE — GSD

1. Relay GSD's spec workflow.

## POST — Forge

1. Any ambiguity touching an art/gameplay interface is high severity regardless of GSD's score, because it blocks parallel DAGs.
