---
name: forge-research
description: Absorb an MCP, Unreal API surface, CLI, DCC tool, model, provider, documentation set, or project corpus into capability contracts, probes, and acceptance links. Use when a tool or knowledge source is added, upgraded, or goes stale.
---

# Forge Research

## Workflow

1. Discover candidate sources without importing them into approved project state. Identify exact source, version, licence, trust boundary, update signal, and intended task classes.
2. Present the discovered set for approval. Prefer authoritative local schemas, help output, reflection, headers, generated API references, and configured tools. Browse only when current external documentation is required.
3. Classify independent sources in bounded parallel packets. Extract decisions, requirements, constraints, conflicts, atomic operations, inputs and outputs, mutation behaviour, lanes, error surfaces, and rollback evidence.
4. Stage conflicts without merging competing acceptance variants. Block destination contracts until the human owner resolves a material contradiction.
5. Create the capability or research contract described in [absorption-contract.md](references/absorption-contract.md).
6. Implement known-good and seeded-bad probes. Compare an optional worker's task quality, complexity ceiling, context use, and verification cost with the resident-host baseline. Never grant `AVAILABLE_VERIFIED` on a detection probe alone.
7. Link enabled workflow steps to fallbacks and acceptance suites. Request capabilities in recipes, never product names.
8. Generate small domain cards, relationship graphs where useful, and retrieval keys. Compile minimal offload packets so local workers never receive whole manuals, the complete GDD, or the resident conversation.
9. Record invalidation triggers and re-probe after a version, schema, model, plugin, engine, hardware, or path change. Route installation and activation through `forge-capability-admin`.
